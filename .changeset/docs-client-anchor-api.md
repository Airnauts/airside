---
"@airnauts/airside-client": patch
---

Docs: fix `captureElement` and `extractSignals` descriptions to match the current `Anchor` and `Signals` types (added `schemaVersion`, `selection?`, and `stableAttrs` fields that were omitted).
