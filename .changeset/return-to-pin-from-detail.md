---
"@airnauts/airside-client": patch
---

The open thread detail's page-context card is now clickable to return to the thread's pin: on the same page it scrolls to and pulses the pin, and when the pin lives on another page it navigates there and re-focuses it on arrival.
