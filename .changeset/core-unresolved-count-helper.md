---
"@airnauts/comments-core": patch
---

Export `unresolvedCountOf(status)` — the domain policy mapping a thread status to
its `unresolvedCount` contribution — so repository adapters share one definition.
