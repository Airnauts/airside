---
"@airnauts/comments-client": patch
---

Sidebar thread now expands to fill the drawer's full height instead of capping the
comment list at a fixed height, so long threads use the available space and scroll
within it. The floating pin popover keeps its compact fixed-height list.
