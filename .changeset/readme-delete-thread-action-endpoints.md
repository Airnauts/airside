---
"@airnauts/airside-core": patch
"@airnauts/airside-server": patch
---

Docs: README updated to document the `DELETE /threads/:id` (delete thread) and `POST /threads/:id/actions/:actionId` (run thread action) endpoints, and the `DeleteThreadResponse` export from `@airnauts/airside-core`.
