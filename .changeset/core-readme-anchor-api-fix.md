---
"@airnauts/airside-core": patch
---

Docs: corrected `scoreCandidate`, `decide`, and `locateQuote` signatures in the README — parameter order, return types, and descriptions now match the source.
