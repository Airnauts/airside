---
"@airnauts/comments-client": patch
---

Sidebar detail polish: the panel header keeps a consistent compact height when opening
a thread (the close button no longer jumps), the reply composer sits directly below the
last message (long threads scroll within the drawer rather than pinning the composer to
the bottom), and the reply input is auto-focused on entry — whether via Reply, a thread
row, or cross-page navigation.
