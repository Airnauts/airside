---
"@airnauts/airside-client": patch
---

Fixed place mode dropping a draft pin on the widget's own chrome: clicking the launcher (open/close panel, exit place mode) or inside the comments sidebar while "Add comment" is active now operates those controls instead of placing a pin.
