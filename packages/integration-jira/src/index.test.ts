import { describe, expect, it } from 'vitest'

// Placeholder so `vitest run` has at least one test until the jiraIssues
// factory and its real spec land in tasks 6.2-6.4.
describe('integration-jira scaffold', () => {
  it('scaffolds', () => {
    expect(true).toBe(true)
  })
})
