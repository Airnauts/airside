# Brainstorming visual-companion scratch (not part of the repo)
.superpowers/

# Dependencies / build
# No trailing slash so worktree node_modules symlinks are ignored too, not just real dirs.
node_modules
dist/

# OS / editor
.DS_Store

# Ephemeral git worktrees (.claude/worktrees/<name>)
.claude/worktrees/

# Local/per-machine agent state (never shared)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Build tooling
.turbo/
*.tsbuildinfo
coverage/

# M5 widget: precompiled Tailwind inlined as a TS string module
packages/client/src/app/widget-css.generated.ts
